% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_calibration_bayes.R
\name{fit_calibration_bayes}
\alias{fit_calibration_bayes}
\title{Fit Bayesian Hierarchical Calibration Curves}
\usage{
fit_calibration_bayes(
  standards,
  samples = NULL,
  blanks = NULL,
  response_var,
  model_names = c("logistic4", "gompertz4"),
  is_log_response = TRUE,
  is_log_independent = TRUE,
  std_curve_conc,
  fixed_a = NULL,
  cv_x_max = 150,
  pcov_threshold = 20,
  min_dynamic_range_log10 = 0.5,
  max_rel_se = 5,
  n_grid = 200L,
  grid_min_conc = 1e-04,
  grid_max_conc = NULL,
  chains = 4L,
  warmup = 1000L,
  sampling = 1000L,
  adapt_delta = 0.9,
  seed = NULL,
  n_draws_predict = 500L,
  n_draws_ensemble = 260L,
  compute_all_grids = FALSE,
  use_heteroscedastic_noise = FALSE,
  run_loo = NULL,
  verbose = FALSE
)
}
\arguments{
\item{standards}{Data frame. Preprocessed stacked standard curve data.
Must contain \code{curve_id}, a response column, and a \code{concentration}
column — all on the fitting scale.}

\item{samples}{Data frame or NULL. Stacked sample data with \code{curve_id}
and the response column (on the raw measurement scale).}

\item{blanks}{Data frame or NULL. Blank well data with \code{curve_id} and
the response column (on the fitting scale). When supplied, blanks are
passed to Stan to anchor the lower asymptote via a separate likelihood
term, and are stored in each per-curve \code{calibration_result$blanks}
slot for downstream QA. Default NULL.}

\item{response_var}{Character. Name of the response column.}

\item{model_names}{Character vector. Models to fit.
Default \code{c("logistic4", "gompertz4")}.}

\item{is_log_response}{Logical. Default TRUE.}

\item{is_log_independent}{Logical. Default TRUE.}

\item{std_curve_conc}{Numeric. Undiluted standard concentration.}

\item{fixed_a}{Numeric or NULL. Fixed lower asymptote (fitting scale).}

\item{cv_x_max}{Numeric. Default 150.}

\item{pcov_threshold}{Numeric. Percent CV threshold for LLOQ/ULOQ
determination and the dynamic-range eligibility gate. Default 20.}

\item{min_dynamic_range_log10}{Numeric. Minimum dynamic range (log10)
for eligibility. Default 0.5.}

\item{max_rel_se}{Numeric. Maximum relative SE (SD/|mean|) permitted
for any parameter. Default 5.0.}

\item{n_grid}{Integer. Default 200.}

\item{grid_min_conc}{Numeric. Default 1e-4.}

\item{grid_max_conc}{Numeric or NULL.}

\item{chains}{Integer. Default 4.}

\item{warmup}{Integer. Default 1000.}

\item{sampling}{Integer. Default 1000.}

\item{adapt_delta}{Numeric. Default 0.9.}

\item{seed}{Integer or NULL.}

\item{n_draws_predict}{Integer. Number of posterior draws for the
best-model grid and sample predictions. Default 500.}

\item{n_draws_ensemble}{Integer. Number of posterior draws for
non-best-model precision grids. Default 260.}

\item{compute_all_grids}{Logical. If TRUE, compute precision
grids for every converged model. Required for eligibility gating
when more than one model is fitted. Default FALSE.}

\item{use_heteroscedastic_noise}{Logical. If TRUE, the Stan models use
a power-of-mean variance function
\verb{sigma_i = exp(log_sigma0 + log_sigma_slope * log(|mu_i|))} in the
likelihood, and the same sigma_i is injected when generating the
CDAN noisy observations in \code{predict_grid_bayes()}. This restores the
O'Malley (2008) CDAN precision profile interpretation. If FALSE
(default), a constant \code{sigma_obs} is used and the precision profiles
reflect posterior-predictive uncertainty driven mainly by inverse-
curve geometry.}

\item{run_loo}{Logical or NULL. Default NULL (auto).}

\item{verbose}{Logical. Default FALSE.}
}
\value{
A \code{calibration_result_multiplate} object (from curveRcore).
Each per-plate \verb{$selection} contains \verb{$assessments}, \verb{$eligible_models},
and \verb{$fallback} from the eligibility gating. Each per-plate
\code{calibration_result} carries \verb{$standards} and \verb{$blanks} slots with
the per-curve subsets of the input data.
}
\description{
Fits a hierarchical Bayesian model to preprocessed standard curve data
across one or more curve_ids simultaneously. Returns a
\code{calibration_result_multiplate} with one entry per curve_id, each
containing its own grid predictions, sample predictions, and
parameter summaries.
}
