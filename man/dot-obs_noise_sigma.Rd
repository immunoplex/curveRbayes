% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_bayes.R
\name{.obs_noise_sigma}
\alias{.obs_noise_sigma}
\title{Observation-noise sigma for one posterior draw (shared by grid + samples)}
\usage{
.obs_noise_sigma(
  mu,
  s,
  use_hetero,
  sigma_obs_draws,
  log_sigma0_draws = NULL,
  log_sigma_slope_draws = NULL
)
}
\arguments{
\item{mu}{Numeric. Mean response the noise is centred on (grid: the forward
mean at the grid point; samples: the observed response, the best available
proxy for the underlying mean).}

\item{s}{Integer. Draw index.}

\item{use_hetero}{Logical. Whether the heteroscedastic model was fitted.}

\item{sigma_obs_draws, log_sigma0_draws, log_sigma_slope_draws}{Numeric draw
vectors (the last two may be NULL under the homoscedastic model).}
}
\description{
Single definition of the per-draw observation SD so the grid and the sample
back-calculation can never diverge. Homoscedastic returns the shared
\code{sigma_obs} draw; heteroscedastic returns the O'Malley power-of-mean form
\verb{exp(log_sigma0 + log_sigma_slope * log(|mu|))}.
}
\keyword{internal}
