% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{bead_assay_example}
\alias{bead_assay_example}
\title{Simulated Bead-Based Immunoassay Example Dataset}
\format{
A named list with the following elements:
\describe{
\item{standards}{Data frame of calibration standards.
Columns: \code{curve_id} (integer), \code{antigen} (character),
\code{plate} (integer), \code{dilution} (numeric),
\code{mfi} (numeric, raw median fluorescence intensity).}
\item{samples}{Data frame of test samples.
Columns: \code{curve_id}, \code{sampleid}, \code{dilution}, \code{mfi}.}
\item{blanks}{Data frame of blank-well readings, or \code{NULL}
if no blanks were recorded.
Columns: \code{curve_id}, \code{mfi}.}
\item{response_var}{Character. Name of the response column
(\code{"mfi"}).}
\item{indep_var}{Character. Name of the independent variable
column used for concentration computation (\code{"dilution"}).}
\item{curve_id_lookup}{Data frame mapping integer \code{curve_id}
values to human-readable labels.
Columns: \code{curve_id}, \code{antigen}, \code{plate}.}
}
}
\source{
Simulated — no real patient data.
}
\description{
A synthetic multi-plate bead-based immunoassay dataset with two
antigens (\code{alpha}, \code{beta}) measured on three plates each, giving
six unique \code{curve_id} values.
Used in \code{vignette("bayesian-quickstart", package = "curveRbayes")}.
}
\details{
Concentrations are simulated on a seven-point dilution series from
0.001 to 30 AU/mL.  The undiluted standard concentration is 30
AU/mL (\code{std_curve_conc = 30}).  Both antigens share the same
dilution scheme but differ in signal level and curve shape to
exercise the inter-antigen variability path of the hierarchical
model.
}
\seealso{
\code{vignette("bayesian-quickstart", package = "curveRbayes")}
}
\keyword{datasets}
